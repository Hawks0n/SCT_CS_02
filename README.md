# SCT_CS_02
A lightweight image encryption and decryption tool built using Python, NumPy, and Pillow (PIL). It performs pixel-level XOR-based encryption, transforming any image into an unintelligible visual pattern that can only be restored with the correct key.  
